services:
  backend:
    build: ./backend
    volumes:
      - ./backend/src:/app/src
      - sqlite_data:/app/data
    ports:
      - "8000:8000"
    # depends_on:
    #   rabbitmq:
    #     condition: service_healthy
    restart: always
    dns:
      - 8.8.8.8
    env_file: .env

  # ML Prediction Microservice
  ml-prediction-service:
    build: ./microservices/ml_prediction_service
    ports:
      - "8001:8001"
    # depends_on:
    #   rabbitmq:
    #     condition: service_healthy
    restart: always
    env_file: .env
    volumes:
      - ./microservices/ml_prediction_service/src/models:/app/src/models

  # NVD Vulnerability Microservice
  nvd-service:
    build: ./microservices/nvd_service
    ports:
      - "8002:8002"
    # depends_on:
    #   rabbitmq:
    #     condition: service_healthy
    restart: always
    env_file: .env
    deploy:
      mode: replicated
      replicas: 1
    volumes:
      - ./microservices/nvd_service/src:/app/src
      - sqlite_data:/app/data
    dns:
      - 8.8.8.8

  # Nmap Scanner Microservice
  nmap-scanner-service:
    build: ./microservices/nmap_scanner
    ports:
      - "8004:8004"
    restart: always
    env_file: .env
    # depends_on: # Removed - using CloudAMQP instead of local RabbitMQ
    #   rabbitmq:
    #     condition: service_healthy
    deploy:
      mode: replicated
      replicas: 1
    environment:
      - NVD_SERVICE_URL=http://nvd-service:8002
      - DATABASE_URL=${DATABASE_URL}
      - RABBITMQ_QUEUE=nmap_scan_queue
      - WORLDTIME_API_URL=${WORLDTIME_API_URL}

    volumes:
      - ./microservices/nmap_scanner/src:/app/src
      - nmap_temp:/app/temp
      - sqlite_data:/app/data
    privileged: true
    cap_add:
      - NET_ADMIN
      - NET_RAW
    dns:
      - 8.8.8.8
      - 8.8.4.4

  # Health Monitor Service
  health-monitor:
    build: ./microservices/health_monitor
    restart: always
    env_file: .env
    depends_on:
      - nmap-scanner-service
    environment:
      - NMAP_SERVICE_URL=http://nmap-scanner-service:8004
      - HEALTH_CHECK_INTERVAL_MS=${HEALTH_CHECK_INTERVAL_MS}
      - ALERT_EMAIL=${ALERT_EMAIL}
      - EMAIL_FROM=${EMAIL_FROM}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
    dns:
      - 8.8.8.8
    # Frontend
  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=${VITE_API_URL:-http://localhost:8000}
        - VITE_BACKEND_URL=${VITE_BACKEND_URL:-http://localhost:8000}
    ports:
      - "5173:80"
    depends_on:
      - backend
      - ml-prediction-service
      - nvd-service
      - nmap-scanner-service
    restart: always
  # Database
  # db:
  #   image: postgres:15
  #   restart: always
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  # Message Queue - REMOVED: Using CloudAMQP instead
  # rabbitmq:
  #   image: rabbitmq:3-management
  #   ports:
  #     - "5672:5672"
  #     - "15672:15672"
  #   restart: always
  #   environment:
  #     RABBITMQ_DEFAULT_USER: guest
  #     RABBITMQ_DEFAULT_PASS: guest
  #   volumes:
  #     - rabbitmq_data:/var/lib/rabbitmq
  #   healthcheck:
  #     test: [ "CMD", "rabbitmq-diagnostics", "-q", "ping" ]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  # pgdata:
  # rabbitmq_data: # Removed - using CloudAMQP
  report_data:
  nmap_temp:
  sqlite_data:
